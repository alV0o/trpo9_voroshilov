<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button"
            x:Key="DeleteButton"
            BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource DeleteButtonColor}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextColorFirst}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <DockPanel HorizontalAlignment="Center"
                                   VerticalAlignment="Center">
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              Margin="10,0,0,0"/>
                            <Label Content="🗑️" Foreground="{TemplateBinding Foreground}"/>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MouseOnDeleteBtn}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextColorSecond}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource PressedDelBtn}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextColorFirst}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>